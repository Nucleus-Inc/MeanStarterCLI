(() => {
  angular.module('AppName').directive('name', () => {
    return {
      restrict: 'AEC',
      require: 'ngModel',
      scope: {
        ngModel: '=ngModel'
      },
      controller: 'NameCtrl as nameCtrl',
      link: (scope, iElement, iAttrs, ngModelCtrl) => {

      }
    }
  })
})()
