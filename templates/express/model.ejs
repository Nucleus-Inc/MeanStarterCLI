const mongoose = require('mongoose')

module.exports = () => {
  const schema = mongoose.Schema({
    title: {
      type: String,
      required: true
    },
    body: {
      type: String,
      required: true
    }
  }, {
    timestamps: true
  })

  return mongoose.model('Post', schema)
}
